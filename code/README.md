# External Code References

This directory is for external reference code only.

Rules:
- Clone or copy third-party repositories only inside `code/`.
- Do not place external references anywhere else in this project tree.
- This directory is git-ignored by default; only this README is tracked.

Use this area for local study/reference material while keeping the main repository clean.
